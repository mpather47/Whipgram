@model IEnumerable<Cellogram.Models.Story>

@{
    ViewData["Title"] = "Home Page";
}

<div class="blog-posts">
    @foreach (var story in Model)
    {
        @await Html.PartialAsync("_Story", story)
    }
</div>